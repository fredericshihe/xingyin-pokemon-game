# 宝箱显示问题分析

## 问题描述
用户反馈：地图上的宝箱只有光环显示，没有宝箱模型显示，拿到后也没有反馈。

## 问题定位

### 1. 宝箱渲染逻辑（ThreeLowPolyMap.jsx:2989-3006）
```javascript
if (eventType === 'item' || eventType === 'pickup') {
  const controller = createDynamicEventDecorationController({
    eventType,
    eventId: typeof object.eventId === 'string' ? object.eventId : null,
    tileX: Math.trunc(Number(object.x)),
    tileY: Math.trunc(Number(object.y)),
    refs,  // 3D模型的引用
    posX: pos.x + (object.offsetX ?? 0),
    posY: object.height ?? 0.2,
    posZ: pos.z + (object.offsetZ ?? 0),
    rotationY: decorationRotationY,
    scale: modelScale,
    signal
  })
  if (controller) {
    controller.syncFromState(mapGrid, collectedEventIdSet)
    dynamicEventDecorations.push(controller)
  }
}
```

### 2. 可见性控制逻辑（ThreeLowPolyMap.jsx:2340-2355）
```javascript
syncFromState(grid, activeCollectedEventIdSet = new Set()) {
  if ((this.eventType === 'item' || this.eventType === 'pickup') && this.eventId) {
    if (activeCollectedEventIdSet.has(this.eventId)) {
      if (!this.collectingStartedAt) this.setVisible(false)
      return
    }
    if (!this.visible || this.collectingStartedAt) {
      this.collectingStartedAt = 0
      this.setVisible(true)
    }
    return
  }
  // ... 其他事件类型的处理
}
```

### 3. 宝箱装饰物生成（godot_region_maps.js:buildThemedPickupDecorations）
```javascript
export function buildThemedPickupDecorations(definition, evt, pickupIndex) {
  const { x, y } = evt.position || {}
  if (!Number.isFinite(x) || !Number.isFinite(y)) return []

  const scenes = PICKUP_VISUAL_SCENES_BY_MAP[definition.id] || DEFAULT_PICKUP_VISUAL_SCENES
  const scene = scenes[pickupIndex % scenes.length]
  
  const mainObject = {
    type: scene.type,  // 例如 'survival_chest', 'platformer_chest'
    x,
    y,
    scale: ...,
    height: ...,
    rotation: ...,
    sourceId: `${evt.id}_pickup_main`,
    eventId: evt.id,        // 关键：绑定事件ID
    eventType: evt.type     // 关键：标记为 'item' 或 'pickup'
  }

  const accentObjects = (scene.accents || []).map((accent, accentIndex) => ({
    type: accent.type,
    x: ...,
    y: ...,
    scale: ...,
    sourceId: `${evt.id}_pickup_accent_${accentIndex + 1}`,
    fixedSceneEventType: evt.type  // 注意：accent用的是fixedSceneEventType
  }))

  return [mainObject, ...accentObjects]
}
```

## 核心问题

### 问题1：宝箱模型可能没有正确加载
**检查点**：
- `scene.type` 是否对应有效的模型key（如 `survival_chest`, `platformer_chest`, `pirate_chest`）
- `getDecorativeModel(object.type)` 是否返回了正确的spec
- `models[spec.key]` 是否存在

**可能原因**：
- 宝箱模型文件未加载
- 模型key映射错误
- 模型加载失败但没有报错

### 问题2：宝箱被错误地隐藏
**检查点**：
- `collectedEventIdSet` 是否包含了不应该包含的ID
- `syncFromState` 是否被正确调用
- 初始状态下 `this.visible` 是否为 true

### 问题3：accent装饰物没有eventId
**问题**：
```javascript
accentObjects = (scene.accents || []).map((accent, accentIndex) => ({
  // ...
  fixedSceneEventType: evt.type  // 只有fixedSceneEventType
  // 缺少 eventId: evt.id
}))
```

**影响**：
- accent装饰物（花、石头等）没有绑定eventId
- 但这些装饰物不应该被 `syncFromState` 控制可见性
- 只有主宝箱模型需要可见性控制

## 解决方案

### 方案1：确保宝箱模型正确加载（优先）

1. **检查模型映射**：确认 `survival_chest`, `platformer_chest`, `pirate_chest` 在 `getDecorativeModel` 中有正确的映射

2. **添加调试日志**：
```javascript
// 在 ThreeLowPolyMap.jsx:2926 附近添加
mapInfo?.decorativeObjects?.forEach((object) => {
  if (object.eventType === 'item' || object.eventType === 'pickup') {
    console.log('[Treasure Debug]', {
      type: object.type,
      eventId: object.eventId,
      position: { x: object.x, y: object.y },
      hasSpec: !!getDecorativeModel(object.type),
      hasModel: !!models[getDecorativeModel(object.type)?.key]
    })
  }
  // ... 原有代码
})
```

3. **检查模型文件**：确认以下文件存在
- `/assets/3d/kenney-survival/chest.glb`
- `/assets/3d/kenney-platformer/chest.glb`
- `/assets/3d/kenney-pirate/chest.glb`

### 方案2：修复可见性控制逻辑

**问题**：如果 `collectedEventIdSet` 初始化时包含了错误的ID，宝箱会被立即隐藏。

**修复**：在 `syncFromState` 中添加调试日志
```javascript
syncFromState(grid, activeCollectedEventIdSet = new Set()) {
  if ((this.eventType === 'item' || this.eventType === 'pickup') && this.eventId) {
    const isCollected = activeCollectedEventIdSet.has(this.eventId)
    console.log('[Treasure Visibility]', {
      eventId: this.eventId,
      isCollected,
      visible: this.visible,
      collecting: !!this.collectingStartedAt
    })
    
    if (isCollected) {
      if (!this.collectingStartedAt) this.setVisible(false)
      return
    }
    if (!this.visible || this.collectingStartedAt) {
      this.collectingStartedAt = 0
      this.setVisible(true)
    }
    return
  }
  // ...
}
```

### 方案3：增强拾取反馈

**当前问题**：拾取动画可能不明显

**改进**：
1. 增加拾取音效（已有 `gameAudio.playUiSelect()`）
2. 增强拾取动画（当前是缩小+上升）
3. 添加粒子效果（可选）

```javascript
// 在 startCollect 中增强反馈
startCollect(now, onCollectVisual = null) {
  if (!this.visible || this.collectingStartedAt) return false
  this.collectingStartedAt = now
  if (this.signal) this.signal.visible = false
  
  // 增强：播放音效
  if (typeof gameAudio !== 'undefined' && gameAudio.playItemCollect) {
    gameAudio.playItemCollect()
  }
  
  onCollectVisual?.(this, now)
  return true
}
```

## 立即行动

### 第一步：添加调试日志（5分钟）
在浏览器控制台查看：
1. 宝箱装饰物是否被正确识别
2. 模型是否加载成功
3. 可见性状态是否正确

### 第二步：检查模型文件（5分钟）
确认宝箱模型文件存在且路径正确

### 第三步：修复问题（根据调试结果）
- 如果模型未加载 → 修复模型路径或添加模型文件
- 如果可见性错误 → 修复 `collectedEventIdSet` 初始化
- 如果反馈不明显 → 增强拾取动画和音效

## 预期结果

修复后：
1. ✅ 宝箱模型正确显示在地图上
2. ✅ 未拾取的宝箱有光环提示
3. ✅ 拾取时有明显的动画和音效反馈
4. ✅ 已拾取的宝箱消失且不再显示
