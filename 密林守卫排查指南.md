# 密林守卫未显示问题排查

## 问题确认

✅ **代码已正确添加**
- 源文件中已有 `valley_trainer_grove_guard`
- 构建文件中已包含守卫代码

## 可能的原因

### 1. 浏览器缓存问题 ⭐ 最可能

**症状：** 浏览器加载的是旧版本的 JS 文件

**解决方案：**
```
1. 打开游戏页面
2. 按 Cmd+Shift+R (Mac) 或 Ctrl+Shift+R (Windows) 强制刷新
3. 或者打开开发者工具 (F12)
4. 右键点击刷新按钮 → 选择"清空缓存并硬性重新加载"
```

### 2. Service Worker 缓存问题 ⭐ 很可能

**症状：** PWA Service Worker 缓存了旧版本

**解决方案：**
```
1. 打开开发者工具 (F12)
2. 进入 Application 标签
3. 左侧选择 Service Workers
4. 点击 "Unregister" 注销 Service Worker
5. 刷新页面
```

### 3. 游戏存档问题

**症状：** 旧存档中已经进入过密林区域

**解决方案：**
```
1. 打开开发者工具 (F12)
2. 进入 Console 标签
3. 输入: localStorage.clear()
4. 输入: indexedDB.deleteDatabase('supabase-db')
5. 刷新页面，重新登录
```

### 4. 开发服务器未重启

**症状：** 开发服务器还在运行旧代码

**解决方案：**
```bash
# 停止开发服务器 (Ctrl+C)
# 重新启动
npm run dev
```

## 完整清理步骤

### 方案 A：快速清理（推荐）

```bash
# 1. 重新构建
npm run build

# 2. 在浏览器中：
# - 打开开发者工具 (F12)
# - Application → Service Workers → Unregister
# - Application → Storage → Clear site data
# - 强制刷新 (Cmd+Shift+R)
```

### 方案 B：完全清理

```bash
# 1. 停止所有服务
# 按 Ctrl+C 停止开发服务器

# 2. 清理构建缓存
rm -rf dist
rm -rf node_modules/.vite

# 3. 重新构建
npm run build

# 4. 启动开发服务器
npm run dev

# 5. 在浏览器中：
# - 清除所有站点数据
# - 注销 Service Worker
# - 清除 localStorage
# - 强制刷新
```

## 验证步骤

### 1. 检查构建文件

```bash
# 确认守卫在构建文件中
grep "valley_trainer_grove_guard" dist/assets/overworld-maps-*.js
# 应该输出: valley_trainer_grove_guard
```

✅ **已确认：守卫在构建文件中**

### 2. 检查浏览器加载的文件

```
1. 打开开发者工具 (F12)
2. 进入 Network 标签
3. 刷新页面
4. 找到 overworld-maps-*.js 文件
5. 查看文件内容，搜索 "valley_trainer_grove_guard"
```

### 3. 检查游戏中的训练师

```
1. 进入游戏
2. 打开开发者工具 Console
3. 输入以下代码查看训练师列表：

// 查看新手村的所有事件
const events = window.__GAME_DEBUG_getMapEvents?.('GodotMap')
console.log(events?.filter(e => e.type === 'trainer'))

// 应该看到 4 个训练师，包括 valley_trainer_grove_guard
```

## 测试守卫是否生效

### 测试步骤

1. **清除存档，重新开始**
   ```javascript
   // 在 Console 中执行
   localStorage.clear()
   location.reload()
   ```

2. **重新登录游戏**
   - 选择初始宝可梦
   - 从营地出发

3. **尝试向右走**
   - 从营地 (x:4, y:27) 向右走
   - 应该在 (x:11, y:23) 遇到密林守卫
   - 守卫会拦住你

4. **查看守卫对话**
   - 对话内容："密林守卫拦住去路：密林草丛野生宝可梦较强，先去阳光草坡练习后再来挑战我！"

5. **查看警告路牌**
   - 在 (x:10, y:21) 应该有路牌
   - 内容："密林入口：野生宝可梦 Lv.4-6，建议先去阳光草坡练习。"

## 如果还是看不到

### Debug 检查清单

- [ ] 确认源文件已修改（已确认 ✅）
- [ ] 确认构建文件包含守卫（已确认 ✅）
- [ ] 清除浏览器缓存
- [ ] 注销 Service Worker
- [ ] 清除 localStorage
- [ ] 重新构建项目
- [ ] 重启开发服务器
- [ ] 使用无痕模式测试

### 最终方案：使用无痕模式

```
1. 打开浏览器无痕模式 (Cmd+Shift+N 或 Ctrl+Shift+N)
2. 访问 http://localhost:3000
3. 登录游戏
4. 测试守卫是否出现
```

无痕模式会忽略所有缓存，如果在无痕模式下能看到守卫，说明问题确实是缓存导致的。

## 预期效果

### 守卫位置

```
营地 (4, 27)
    ↓ 向上
阳光草坡入口
    ↓ 向右
密林守卫 (11, 23) ← 应该在这里看到守卫
    ↓ 击败后
密林草丛 (12-20, 24-30)
```

### 视觉效果

- 守卫应该显示为一个 NPC 角色模型
- 站在 (11, 23) 位置
- 面朝下方 (facing: 'down')
- 阻挡玩家通过

---

## 总结

**问题原因：** 最可能是浏览器缓存或 Service Worker 缓存了旧版本

**解决方案：**
1. 强制刷新浏览器 (Cmd+Shift+R)
2. 注销 Service Worker
3. 清除站点数据
4. 使用无痕模式测试

**验证方法：** 从营地向右走，应该在 (11, 23) 遇到密林守卫
