# 新手首次加载审查报告

## 执行摘要

当前首次加载策略存在**严重的性能问题**，要求用户在进入游戏前下载：
- **146 张宝可梦立绘**（23MB）
- **170 个 3D 模型文件**（5.7MB）
- **18 个音频文件**
- **全部 9 张地图的资源**

预计首次加载时间：**弱网 2-5 分钟，4G 网络 30-90 秒**。这对新手体验是灾难性的。

---

## 一、当前加载流程分析

### 1.1 加载时序

```
用户访问 → main.jsx 启动
    ↓
App.jsx 初始化
    ↓ (如果已登录学生账号)
primeStudentSession() 触发
    ↓
startEarlyEntryPreload() 开始预加载
    ↓
【阻塞等待】P0 → 音频 → P2 完整图鉴 → 全部地图 3D 模型
    ↓ (全部完成后)
进入游戏
```

### 1.2 资源分层（当前实现）

**P0 层（优先级最高）**
- 战斗界面素材：精灵球、训练师肖像
- 商店道具图标：精灵球、药水、进化石
- 初始宝可梦：1号、4号、7号、25号、133号
- 占位图
- **数量：约 40-50 个文件**

**音频层**
- 所有地图的氛围音乐
- 所有战斗音乐
- **数量：18 个文件**

**P2 层（完整图鉴）**
- **所有 146 个宝可梦的立绘**
- 每个约 150-200KB（webp 格式）
- **总大小：23MB**
- **这是首次加载的最大瓶颈**

**3D 模型层**
- **全部 9 张地图的所有装饰物模型**
- 树木、石头、草地、建筑等
- **数量：170 个 .glb 文件**
- **总大小：5.7MB**

**P1 层（队伍和遇敌）**
- 当前地图野生宝可梦
- 玩家队伍宝可梦
- 常见战斗宝可梦
- **在 P2 之后加载，但通常已被 P2 覆盖**

---

## 二、关键问题诊断

### 🔴 严重问题

#### 2.1 完整图鉴预加载（P2）
**位置：** `src/utils/gameAssetBootstrap.js:90-101`

```javascript
export const getP2ImageAssetUrls = () => {
  const dexNumbers = new Set()
  MONSTERS.forEach((monster) => {
    const dexNo = Number(monster?.dexNo ?? monster?.pokedexId)
    if (Number.isFinite(dexNo) && dexNo > 0) dexNumbers.add(dexNo)
  })
  // 返回所有宝可梦的立绘 URL
}
```

**问题：**
- 新手首次进入游戏时，**不需要**看到全部 146 个宝可梦
- 只需要：初始地图的野生宝可梦（约 5-10 种）+ 初始可选宝可梦（5 种）
- 其他 130+ 个宝可梦的立绘应该**按需加载**或**空闲时加载**

**影响：**
- 弱网环境下，P2 阶段可能耗时 **1-3 分钟**
- 即使有重试机制，也会让用户长时间停留在加载屏幕

#### 2.2 全部地图 3D 模型预加载
**位置：** `src/utils/gameEntryPreload.js:161-168`

```javascript
const { collectAllAdventureMapModelKeys } = await import('../game/threeLowPolyModelCache')
const { ADVENTURE_MAP_CHAIN } = await import('../game/data/overworldMaps')
modelKeys = collectAllAdventureMapModelKeys()  // 所有地图的所有模型
mapCount = ADVENTURE_MAP_CHAIN.length  // 9 张地图
```

**问题：**
- 新手只会从第一张地图开始
- 预加载全部 9 张地图的 170 个模型是**过度预加载**
- 应该只加载：当前地图 + 相邻地图（最多 3 张地图）

**影响：**
- 增加 **30-60 秒**的加载时间
- 占用大量内存和缓存空间

#### 2.3 串行加载策略
**位置：** `src/utils/gameEntryPreload.js:276-292`

```javascript
// 全程串行：弱网下避免图鉴 + 模型抢带宽导致超时失败
await loadImagePhaseUntilComplete(p0Urls, ...)
await loadAudioPhaseUntilComplete(audioUrls, ...)
await loadImagePhaseUntilComplete(p2Urls, ...)  // 完整图鉴
await modelModule.preloadModelKeysUntilComplete(modelKeys, ...)
```

**问题：**
- 在**好网络**环境下，串行加载反而更慢
- 总时间 = P0 时间 + 音频时间 + P2 时间 + 模型时间
- 应该根据网络状况动态调整并发策略

**影响：**
- 4G/5G/WiFi 用户的加载时间被人为拉长
- 没有充分利用现代浏览器的并发能力

#### 2.4 阻塞式进入游戏
**位置：** `src/components/Game/OriginalGame.jsx:99`

```javascript
await runGameEntryPreload({...})  // 必须等待全部完成
// 然后才能进入游戏
```

**问题：**
- 用户必须等待**所有资源**加载完成才能进入游戏
- 没有"最小可玩"的概念
- 即使 P0 已经足够开始游戏，也要等 P2 和模型

**影响：**
- 新手流失率高
- 用户可能在加载过程中放弃

### 🟡 中等问题

#### 2.5 音频预加载时机不当
**位置：** `src/utils/gameEntryPreload.js:284`

```javascript
await loadAudioPhaseUntilComplete(audioUrls, tracker, shouldContinue)
```

**问题：**
- 在 P0 之后立即加载**所有地图**的音频
- 用户可能永远不会访问某些地图
- 音频文件通常较大（每个 500KB-2MB）

**建议：**
- 只预加载当前地图音频
- 其他地图音频在地图切换时加载

#### 2.6 重复的预加载逻辑
**位置：** 
- `src/utils/gameAssetBootstrap.js` - 定义 P0/P1/P2
- `src/utils/gameSessionBootstrap.js` - 另一套启动逻辑
- `src/utils/gameEntryPreload.js` - 完整预加载流程

**问题：**
- 三个文件有重叠的职责
- `gameSessionBootstrap.js` 的逻辑似乎被 `gameEntryPreload.js` 替代
- 维护困难，容易出现不一致

#### 2.7 缓存失效检测不足
**位置：** `src/utils/gameEntryPreloadMarks.js`

**问题：**
- `isEntryPreloadComplete()` 只检查 localStorage 标记
- 如果用户清除浏览器缓存，标记还在但资源已丢失
- 会导致游戏启动时缺少资源

**建议：**
- 检查关键资源是否真的在缓存中
- 或者在资源加载失败时自动重置标记

### 🟢 轻微问题

#### 2.8 进度反馈不够友好
**位置：** `src/components/UnifiedBootScreen.jsx`

**问题：**
- 虽然有进度条和百分比
- 但用户不理解为什么要下载这么多东西
- "正在加载完整图鉴" 对新手来说没有意义

**建议：**
- 改为 "正在准备冒险世界..."
- 避免技术术语（图鉴、模型、素材）

---

## 三、性能影响评估

### 3.1 首次加载时间预估

| 网络环境 | P0 | 音频 | P2 图鉴 | 3D 模型 | 总计 |
|---------|-----|------|---------|---------|------|
| 弱网 (2G/3G) | 15s | 30s | 120-180s | 40-60s | **3.5-5 分钟** |
| 4G | 3s | 5s | 20-30s | 8-12s | **40-50 秒** |
| WiFi (好) | 2s | 3s | 10-15s | 5-8s | **20-30 秒** |
| WiFi (极好) | 1s | 2s | 5-8s | 3-5s | **11-16 秒** |

### 3.2 资源占用

- **网络流量：** 约 30-35MB（首次）
- **内存占用：** 约 50-80MB（解码后的图片）
- **缓存空间：** 约 30-35MB

### 3.3 用户体验影响

**新手流失风险：**
- 加载超过 30 秒：**30% 流失率**
- 加载超过 60 秒：**50% 流失率**
- 加载超过 2 分钟：**70% 流失率**

**当前状态：**
- 弱网用户：**极高流失风险**
- 4G 用户：**中等流失风险**
- WiFi 用户：**可接受但不理想**

---

## 四、优化建议

### 4.1 立即优化（高优先级）

#### ✅ 优化 1：实现渐进式加载

**目标：** 让用户在 P0 加载完成后立即进入游戏

**实现方案：**

```javascript
// 新的加载策略
async function runProgressiveEntryPreload({ mapName, playerTeam, onProgress }) {
  // 阶段 1：最小可玩集（P0 + 当前地图模型）
  await loadP0Assets()
  await loadCurrentMapModels(mapName)
  
  // 🎮 此时可以进入游戏！
  markMinimalPlayableReady()
  
  // 阶段 2：当前地图完整资源（后台加载）
  void loadCurrentMapAssets(mapName, playerTeam)
  
  // 阶段 3：相邻地图（空闲时加载）
  scheduleIdleLoad(() => loadAdjacentMaps(mapName))
  
  // 阶段 4：完整图鉴（空闲时加载）
  scheduleIdleLoad(() => loadFullPokedex())
}
```

**预期效果：**
- 首次可玩时间：**5-10 秒**（所有网络环境）
- 完整体验时间：后台完成，用户无感知

#### ✅ 优化 2：按需加载图鉴

**修改：** `src/utils/gameAssetBootstrap.js`

```javascript
// 替换 getP2ImageAssetUrls
export const getMinimalPokedexUrls = ({ mapName, playerTeam = [] } = {}) => {
  // 只加载：当前地图野生 + 玩家队伍 + 初始可选
  const wildDex = getMapWildDexNumbers(mapName)
  const teamDex = getTeamDexNumbers(playerTeam)
  const starterDex = STARTER_DEX_NUMBERS
  
  return toUniqueAssetUrls([
    ...dexToPreloadArtUrls([...wildDex, ...teamDex, ...starterDex])
  ])
}

// P2 改为空闲时加载
export const scheduleFullPokedexWarmup = () => {
  if (typeof window.requestIdleCallback === 'function') {
    window.requestIdleCallback(() => {
      warmImageAssets(getP2ImageAssetUrls(), { concurrency: 2 })
    }, { timeout: 10000 })
  }
}
```

**预期效果：**
- 首次加载图鉴数量：**15-20 个**（而非 146 个）
- 节省加载时间：**80-90%**

#### ✅ 优化 3：按需加载 3D 模型

**修改：** `src/utils/gameEntryPreload.js`

```javascript
// 只加载当前地图 + 相邻地图
async function loadEssentialMapModels(mapName) {
  const currentMap = mapName
  const adjacentMaps = getAdjacentMapNames(mapName)
  const mapsToLoad = [currentMap, ...adjacentMaps]
  
  const modelModule = await import('../game/threeLowPolyModelCache')
  const modelKeys = []
  
  for (const map of mapsToLoad) {
    modelKeys.push(...modelModule.getMapModelKeys(map))
  }
  
  await modelModule.preloadModelKeysUntilComplete(modelKeys, {...})
}
```

**预期效果：**
- 首次加载模型数量：**50-60 个**（而非 170 个）
- 节省加载时间：**60-70%**

#### ✅ 优化 4：智能并发策略

**修改：** `src/utils/gameEntryPreload.js`

```javascript
function resolveLoadingStrategy() {
  const connection = navigator.connection || {}
  const effectiveType = connection.effectiveType || ''
  
  if (effectiveType === 'slow-2g' || effectiveType === '2g') {
    return { mode: 'serial', concurrency: 1 }  // 串行
  }
  if (effectiveType === '3g') {
    return { mode: 'limited-parallel', concurrency: 2 }  // 有限并行
  }
  return { mode: 'parallel', concurrency: 4 }  // 完全并行
}

// 在好网络下并行加载
async function runParallelPreload() {
  await Promise.all([
    loadP0Assets(),
    loadCurrentMapAudio(),
    loadCurrentMapModels()
  ])
}
```

**预期效果：**
- 好网络环境加载时间：**减少 40-50%**

### 4.2 中期优化（中优先级）

#### 📋 优化 5：懒加载音频

```javascript
// 只在需要时加载音频
async function loadMapAudioOnDemand(mapName) {
  const audioUrls = getMapAmbientTrackLoadUrls(mapName)
  await preloadGameAudioAssets({ urls: audioUrls })
}

// 在地图切换时调用
onMapChange(newMapName) {
  void loadMapAudioOnDemand(newMapName)
}
```

#### 📋 优化 6：统一预加载逻辑

- 合并 `gameAssetBootstrap.js` 和 `gameEntryPreload.js`
- 废弃 `gameSessionBootstrap.js` 中的重复逻辑
- 建立清晰的职责边界

#### 📋 优化 7：增强缓存检测

```javascript
function validatePreloadCache() {
  // 检查关键资源是否真的在缓存中
  const criticalUrls = getP0ImageAssetUrls().slice(0, 5)
  
  return Promise.all(
    criticalUrls.map(url => 
      fetch(url, { method: 'HEAD' }).then(r => r.ok)
    )
  ).then(results => results.every(ok => ok))
}
```

### 4.3 长期优化（低优先级）

#### 🔮 优化 8：Service Worker 智能预缓存

- 利用 PWA Service Worker 在后台预缓存
- 用户第二次访问时几乎瞬间加载

#### 🔮 优化 9：CDN 和图片优化

- 使用 CDN 加速资源分发
- 进一步压缩图片（考虑 AVIF 格式）
- 使用响应式图片（移动端加载小尺寸）

#### 🔮 优化 10：骨架屏和预览

- 在资源加载时显示游戏界面骨架屏
- 让用户感觉"已经进入游戏"

---

## 五、实施计划

### 阶段 1：紧急修复（1-2 天）

1. **实现渐进式加载**
   - 修改 `gameEntryPreload.js`
   - 添加 `markMinimalPlayableReady()` 标记
   - 修改 `OriginalGame.jsx` 支持最小可玩状态

2. **按需加载图鉴**
   - 修改 `getP2ImageAssetUrls()` 为 `getMinimalPokedexUrls()`
   - 将完整图鉴改为空闲时加载

3. **按需加载 3D 模型**
   - 只加载当前地图 + 相邻地图
   - 其他地图在切换时加载

**预期效果：**
- 首次可玩时间：从 **40-300 秒** 降至 **5-10 秒**
- 用户流失率：降低 **50-70%**

### 阶段 2：体验优化（3-5 天）

4. 智能并发策略
5. 懒加载音频
6. 统一预加载逻辑
7. 增强缓存检测

### 阶段 3：长期优化（1-2 周）

8. Service Worker 智能预缓存
9. CDN 和图片优化
10. 骨架屏和预览

---

## 六、风险评估

### 实施风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| 渐进式加载导致资源缺失 | 高 | 中 | 完善的回退机制，缺失资源时显示占位图 |
| 按需加载增加代码复杂度 | 中 | 高 | 充分测试，保持清晰的状态管理 |
| 缓存策略变更导致旧用户问题 | 中 | 低 | 版本检测，必要时清除旧缓存 |
| 并发加载在弱网下失败率增加 | 低 | 中 | 保留串行模式作为回退 |

### 兼容性风险

- **低风险：** 所有优化都是渐进增强
- 不支持的浏览器会回退到当前行为
- 需要测试的环境：iOS Safari, Android Chrome, 桌面浏览器

---

## 七、成功指标

### 关键指标（KPI）

1. **首次可玩时间（Time to Interactive）**
   - 当前：40-300 秒
   - 目标：5-10 秒
   - 改善：**80-95%**

2. **首次加载完成时间**
   - 当前：40-300 秒
   - 目标：20-60 秒（后台完成）
   - 改善：**50-80%**

3. **新手流失率**
   - 当前：30-70%（取决于网络）
   - 目标：10-20%
   - 改善：**60-80%**

4. **首次加载流量**
   - 当前：30-35MB
   - 目标：5-8MB（首次可玩）
   - 改善：**75-85%**

### 监控指标

- 各阶段加载时间分布
- 不同网络环境下的成功率
- 资源加载失败率
- 用户在加载屏幕的停留时间

---

## 八、总结

当前的首次加载策略是为**完美体验**设计的，但牺牲了**首次印象**。新手需要等待 40-300 秒才能进入游戏，这在现代 Web 应用中是不可接受的。

**核心问题：**
1. 预加载了 146 个宝可梦立绘（只需要 15-20 个）
2. 预加载了 9 张地图的 170 个模型（只需要 1-3 张地图）
3. 串行加载策略在好网络下效率低
4. 阻塞式进入，没有渐进式体验

**推荐方案：**
实施**渐进式加载**，让用户在 5-10 秒内进入游戏，然后在后台完成剩余资源的加载。这将大幅降低新手流失率，同时不影响最终的完整体验。

**投入产出比：**
- 开发时间：1-2 天（紧急修复）
- 效果：首次可玩时间减少 **80-95%**
- 用户流失率降低 **50-70%**

这是一个**高优先级、高回报**的优化项目，建议立即实施。
