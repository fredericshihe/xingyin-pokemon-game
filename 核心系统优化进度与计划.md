# 核心系统优化进度与计划

更新时间：2026-06-03

## 当前结论

本轮优先处理了经验溢出、云端唯一进度、地图玩法审计、战斗视觉流程、战斗背景、难度、成长曲线、经验经济和 MP 合理性。当前最高优先级问题已经收敛；隐藏区专属强力宝可梦保持“能 1 打 3”的奖励生态设计，不作为等级异常削弱。

## 已完成

| 问题 | 处理 | 验证 |
|---|---|---|
| 经验爆表但不升级 | 批量旧存档规范化已能清理大额溢出；伊布溢出和小火龙逾期进化均有审计覆盖 | `npm run audit:exp-overflow` 通过，`remainingOverflow: []` |
| 云端唯一进度误报 | 进度类云同步与设备偏好/缓存白名单边界已收口 | `npm run audit:cloud` 通过，violations `0` |
| 地图区域和玩法审计失败 | 隐藏区入口、外圈、可达性、宝箱价值、区域布局均通过审计 | `map:audit-regions`、`map:audit-gameplay`、`map:audit-hidden-zones` 通过 |
| 战斗二段动画风险 | 二段效果不再移动攻击方；技能视觉事件按阶段清理 | `npm run audit:battle-visual` 通过 |
| 战斗背景审计脱节 | 审计脚本展开 `index.css` 的导入链；补齐 8 个隐藏区场景映射；reward/minigame 视觉层级映射到 normal | `npm run audit:battle-scenes` 通过 |
| 无伤害招式误判 | 百变怪的“变身/模仿”按运行时可适应伤害来源处理，保留官方设定 | `npm run audit:difficulty` 通过，`noDamagingMoveCount: 0` |
| 65 只宝可梦缺官方基础经验 | 重新联网生成 `officialExperience.js` 和 `officialMoveMeta.js`，覆盖 209 只宝可梦 | `npm run audit:economy` 通过，`missingExperienceDataCount: 0` |
| 高等级 MP 建议项 | 生命水滴 `7->12`、光合作用 `10->15`、原始之力 `13->15`，并写入生成脚本防回归 | `npm run audit:mp` 通过，critical/advisory 均为 `0` |
| 技能消耗成本模型偏差 | 龙尾 `10->7`，击掌奇袭 `8->12`，并写入生成脚本防回归；经济审计新增实际地图遭遇表维度 | `npm run audit:economy` 通过，`moveCostWarningCount: 0`，`moveAffordabilityWarningCount: 0` |
| 升级成长曲线和进化/学招风险 | 对 209 只宝可梦做 1-100 级模拟，检查经验曲线、属性回退、连续进化和进化后学招 | `npm run audit:growth` 通过，20900 个等级模拟异常为 `0` |

## 隐藏区设计口径

隐藏区不是普通草丛的平替，而是付费开启后的强奖励生态：

- 入口统一为 `100` 金币。
- 每个隐藏区只有一个入口，其余外圈必须被清晰模型封住。
- 每个隐藏区有高价值宝箱和 3 只专属强力宝可梦。
- 专属强力宝可梦要求能击败当前地图 Boss 前 3 只，作为稀有奖励的爽点。
- 这个强度会通过 `map:audit-hidden-zones` 里的 boss overmatch 审计保护，不应被地图等级差审计误削弱。

对应文档：`所有地图宝可梦分配与概率说明.md` 与 `所有地图宝可梦分配与概率说明-审查报告.md`。

## 当前保留观察项

| 项目 | 状态 | 最优后续 |
|---|---|---|
| 铁木营地金币小游戏 6 只队伍 warning | 设计允许。小游戏是完整练习赛，不按普通训练师 2-3 只限制 | 后续可给审计脚本增加 `minigame` 专用说明，不降级队伍 |
| 经济审计 fastLevelingScenario | 已拆分为理论压力测试和实际地图样本：理论 `96`，实际地图 `61`，其中普通野区 `55`、7 个付费隐藏区 `6`，慢升级 `0`；早期普通野区快升级是新手节奏，隐藏区快升级是奖励设计 | 后续只有当实测新手升级过快时再微调早期普通野区权重或经验奖励，不削弱隐藏区一打三定位 |
| chunk / PWA 体积 | `npm run build:check` 通过，但 vendor-three 约 630KB、GameWrapper 约 449KB、PWA precache 约 144MB，仍会影响首次加载和旧缓存更新 | 分阶段拆 Three 运行时、模型 manifest、战斗/背包面板和 PWA runtime caching |
| 大文件维护压力 | `OriginalGame.jsx`、`game.css`、`godot_region_maps.js` 仍过大 | 先抽审计已覆盖的稳定模块：战斗视觉、战斗结算、地图事件、UI 面板、地图生成数据 |
| 网络类安全审计 | 已联网补跑 | `npm audit --omit=dev` 通过，0 vulnerabilities；`npm run audit:pokemon-names` 通过，209 只无重复图鉴号/缺官方名/中文名不一致 |

## 回归命令清单

本轮已通过：

- `npm run audit:exp-overflow`
- `npm run audit:cloud`
- `npm run map:audit-regions`
- `npm run map:audit-gameplay`
- `npm run map:audit-hidden-zones`
- `npm run audit:battle-visual`
- `npm run audit:battle-scenes`
- `npm run audit:difficulty`
- `npm run audit:economy`
- `npm run audit:mp`
- `npm run audit:growth`
- `npm audit --omit=dev`
- `npm run audit:pokemon-names`
- `npm run build:check`

建议后续涉及对应模块时按需运行：

- 改地图/隐藏区：`npm run map:audit-regions && npm run map:audit-gameplay && npm run map:audit-hidden-zones`
- 改宝可梦分布：`npm run generate:pokemon-distribution-doc && npm run map:audit-pokemon && npm run audit:dex-acquisition`
- 改战斗视觉/技能：`npm run audit:battle-visual && npm run audit:battle-scenes && npm run audit:difficulty && npm run audit:mp`
- 改升级/经验/奖励：`npm run audit:exp-overflow && npm run audit:economy && npm run audit:growth`
