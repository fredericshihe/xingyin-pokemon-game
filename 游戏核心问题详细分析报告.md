# 游戏核心问题详细分析报告

**分析时间**: 2026-05-31  
**分析深度**: 代码级别  
**问题数量**: 4个

---

## 问题1: 泉水3D模型统一 ✅

### 分析结果
**文件**: `src/game/data/godotMaps/godot_region_maps.js`  
**函数**: `buildFixedHealingSpringScene` (第5038行)

**代码**:
```javascript
function buildFixedHealingSpringScene(evt) {
  return [
    makeFixedSceneObject(evt, 'spring_fountain', 'town_fountain_round', 0, 0, {
      scale: 1.08,
      height: 0.18,
      eventType: 'heal'
    }),
    // ... 其他装饰物
  ]
}
```

**结论**: ✅ 所有泉水使用统一的`town_fountain_round`模型，无需修改

---

## 问题2: 宝箱拾取后不消失 🔴

### 深度分析

#### 渲染流程
1. **decorativeObjects渲染** (第2933行)
   - 遍历所有装饰物
   - 渲染宝箱模型和发光环
   - 将eventId添加到`signaledEventIds`

2. **runtimeEvents渲染** (第3060行)
   - 遍历所有运行时事件
   - 如果eventId在`signaledEventIds`中，跳过
   - 否则渲染事件信号

#### 问题根源

**文件**: `src/game/ThreeLowPolyMap.jsx`  
**位置**: 第2933-3025行

**当前代码**:
```javascript
mapInfo?.decorativeObjects?.forEach((object) => {
  decorationStats.total++
  const spec = getDecorativeModel(object.type)
  if (!spec) return
  if (!models[spec.key]) return
  
  // ❌ 问题：没有检查collectedEventIdSet
  // 即使宝箱已拾取，仍然会渲染
  
  if (object.eventType === 'item' || object.eventType === 'pickup') {
    decorationStats.treasures++
    // 渲染宝箱...
  }
  
  // 渲染装饰物和信号
  const signal = addEventSignalAt(...)
  const refs = addInstance(...)
  
  if (eventType && typeof object.eventId === 'string') {
    signaledEventIds.add(object.eventId)
  }
})
```

**问题**: 
- `collectedEventIdSet`已经存在（第1721行定义）
- 但在渲染decorativeObjects时**没有使用它来过滤**
- 导致已拾取的宝箱仍然被渲染

#### 修复方案

**在第2933行之后添加过滤**:
```javascript
mapInfo?.decorativeObjects?.forEach((object) => {
  decorationStats.total++
  
  // ✅ 新增：过滤已拾取的宝箱
  if ((object.eventType === 'item' || object.eventType === 'pickup') && 
      typeof object.eventId === 'string' && 
      collectedEventIdSet.has(object.eventId)) {
    decorationStats.skippedCollected = (decorationStats.skippedCollected || 0) + 1
    return  // 跳过已拾取的宝箱
  }
  
  const spec = getDecorativeModel(object.type)
  // ... 其余代码不变
})
```

---

## 问题3: 宝箱遮挡人物 🟡

### 深度分析

#### 渲染顺序
**文件**: `src/game/ThreeLowPolyMap.jsx`  
**位置**: 第2982行

**当前代码**:
```javascript
const modelLift = object.height ?? 0.2
```

**问题**:
- 宝箱的默认height是0.2（在地面上方）
- 人物的渲染顺序可能在宝箱之前
- 导致宝箱遮挡人物

#### 修复方案

**方案A: 调整宝箱height为负值**
```javascript
const modelLift = (object.eventType === 'item' || object.eventType === 'pickup')
  ? (object.height ?? -0.05)  // 宝箱在地面下，不遮挡人物
  : (object.height ?? 0.2)
```

**方案B: 调整信号的baseY**
```javascript
const signalBaseY = eventType
  ? getEventSignalBaseY(eventType, object.npcRole, modelScale, modelLift, models[spec.key])
  : 0.02

// 如果是宝箱，降低信号高度
if (eventType === 'item' || eventType === 'pickup') {
  signalBaseY = Math.min(signalBaseY, 0.1)  // 限制最大高度
}
```

**推荐**: 方案A，简单直接

---

## 问题4: 逃跑后回到起点 🔴

### 深度分析

#### 战斗退出流程
**文件**: `src/components/Game/OriginalGame.jsx`  
**位置**: 第7680行

**当前代码**:
```javascript
const buildExitedBattleSnapshot = (baseSnapshot, overrides = {}) => {
  const exitedTeam = Array.isArray(overrides.playerTeam)
    ? overrides.playerTeam
    : (Array.isArray(baseSnapshot?.playerTeam) ? baseSnapshot.playerTeam : []);
  const defaultActivePlayerId = resolveDefaultActivePlayerId(exitedTeam, baseSnapshot?.activePlayerId);

  return {
    ...baseSnapshot,
    view: 'map',
    turn: 'player',
    participatedMonIds: [],
    enemyTeam: [],
    activeEnemyId: null,
    activePlayerId: defaultActivePlayerId,
    gameOver: false,
    battleKind: 'wild',
    battlePhase: 'active',
    battlePhaseData: null,
    pendingBattleSwitch: null,
    isThrowingPokeball: false,
    captureSequenceData: null,
    encounterCooldownSteps: ENCOUNTER_SAFE_STEPS,
    activeBattleEnergyCost: 0,
    battleEnergyRefundEligible: false,
    ...overrides,
  };
};
```

**问题**:
- 函数返回`...baseSnapshot`，理论上应该保留`playerPos`
- 但实际测试中玩家回到了起点
- 可能是`baseSnapshot`本身的`playerPos`已经被重置

#### 根本原因

需要检查战斗开始时的快照构建，可能在那里`playerPos`被重置了。

#### 修复方案

**方案A: 显式保留playerPos**
```javascript
const buildExitedBattleSnapshot = (baseSnapshot, overrides = {}) => {
  // ... 现有代码
  
  return {
    ...baseSnapshot,
    view: 'map',
    playerPos: baseSnapshot.playerPos,  // ✅ 显式保留位置
    // ... 其他字段
    ...overrides,
  };
};
```

**方案B: 在战斗开始时保存preBattlePos**

需要找到战斗开始的代码，添加：
```javascript
const startBattle = (snapshot) => {
  return {
    ...snapshot,
    view: 'battle',
    preBattlePos: { ...snapshot.playerPos },  // 保存战斗前位置
    // ... 其他战斗数据
  }
}
```

然后在`buildExitedBattleSnapshot`中恢复：
```javascript
playerPos: baseSnapshot.preBattlePos || baseSnapshot.playerPos,
preBattlePos: null,
```

**推荐**: 先尝试方案A（最简单），如果不行再用方案B

---

## 修复优先级和顺序

### 第1步: 修复宝箱拾取后不消失（最关键）
- 文件: `src/game/ThreeLowPolyMap.jsx`
- 位置: 第2933行之后
- 改动: 添加3-5行代码

### 第2步: 修复逃跑后回到起点（严重影响体验）
- 文件: `src/components/Game/OriginalGame.jsx`
- 位置: 第7680行
- 改动: 添加1行代码

### 第3步: 修复宝箱遮挡人物（视觉优化）
- 文件: `src/game/ThreeLowPolyMap.jsx`
- 位置: 第2982行
- 改动: 修改1行代码

---

## 代码修改清单

### 修改1: ThreeLowPolyMap.jsx (第2933行)
```javascript
// 在第2933行的forEach内部，第2934行之后添加：
mapInfo?.decorativeObjects?.forEach((object) => {
  decorationStats.total++
  
  // ✅ 新增：过滤已拾取的宝箱
  if ((object.eventType === 'item' || object.eventType === 'pickup') && 
      typeof object.eventId === 'string' && 
      collectedEventIdSet.has(object.eventId)) {
    decorationStats.skippedCollected = (decorationStats.skippedCollected || 0) + 1
    return
  }
  
  // 原有代码继续...
  const spec = getDecorativeModel(object.type)
```

### 修改2: ThreeLowPolyMap.jsx (第2982行)
```javascript
// 修改第2982行：
// 原代码：
const modelLift = object.height ?? 0.2

// 新代码：
const modelLift = (object.eventType === 'item' || object.eventType === 'pickup')
  ? (object.height ?? -0.05)
  : (object.height ?? 0.2)
```

### 修改3: OriginalGame.jsx (第7688行)
```javascript
// 在第7688行之后添加：
return {
  ...baseSnapshot,
  view: 'map',
  playerPos: baseSnapshot.playerPos,  // ✅ 新增：显式保留位置
  turn: 'player',
  // ... 其余代码不变
```

---

## 测试验证清单

### 测试1: 宝箱拾取
- [ ] 拾取宝箱后，发光环立即消失
- [ ] 重新进入地图，宝箱不再出现
- [ ] 浏览器控制台无错误

### 测试2: 逃跑位置
- [ ] 在地图任意位置触发战斗
- [ ] 逃跑成功
- [ ] 确认回到战斗前的位置（不是起点）

### 测试3: 宝箱遮挡
- [ ] 站在宝箱上
- [ ] 人物应该遮挡宝箱（不是被宝箱遮挡）

---

## 风险评估

### 修改1: 宝箱过滤
- **风险**: 低
- **影响范围**: 仅影响宝箱渲染
- **回滚**: 删除添加的代码即可

### 修改2: 宝箱高度
- **风险**: 低
- **影响范围**: 仅影响宝箱视觉
- **回滚**: 恢复原值0.2

### 修改3: 逃跑位置
- **风险**: 中
- **影响范围**: 战斗退出逻辑
- **回滚**: 删除添加的代码
- **注意**: 需要测试胜利/失败是否也受影响

---

**分析完成时间**: 2026-05-31  
**准备执行修复**: 是  
**预计修复时间**: 5分钟
